package cs3500.pa05.view;

import javafx.fxml.FXMLLoader;
import javafx.stage.Stage;

/**
 * Implementation of the view that extends the Abstract View
 */
public class ViewImpl extends AbstractView {

  /**
   * Instantiates a new ViewImpl
   *
   * @param loader Loader to load the scene
   * @param stage the main stage
   */
  public ViewImpl(FXMLLoader loader, Stage stage) {
    super(loader, stage);
  }
}
